import Head from 'next/head'

export default function Landing(): JSX.Element {
    return <div>
        <Head>
            <title>Landing &minus; Link Shortner</title>
            <meta name="description" content="Shorten your links and share them all over the world" />
        </Head>
        <p>landing page</p>
    </div>
}